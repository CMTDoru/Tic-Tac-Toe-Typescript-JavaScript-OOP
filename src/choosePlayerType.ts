// No uses for this class at the moment.

// import { Color } from "./types";
// import { PlayerType } from './playertype';

// export default class ChoosePlayerType {
//     id!: string;
//     padding: number;
//     borderRadius: number;
//     bgColor: Color;
//     typePlayer: PlayerType

//     constructor(padding: number, borderRadius: number, bgColor: Color, typePlayer: PlayerType) {
//         this.padding = padding;
//         this.borderRadius = borderRadius;
//         this.bgColor = bgColor;
//         this.typePlayer = typePlayer;

//         const divButton = document.querySelector('.player-type') as HTMLDivElement
//         const buttons: HTMLButtonElement = document.createElement('button')

//         buttons.style.padding = `${padding}px`;
//         buttons.style.borderRadius = `${borderRadius}px`;
//         buttons.style.backgroundColor = bgColor;
//         buttons.style.margin = "10px"
//         buttons.style.color = "#fff";
//         buttons.style.fontSize = "18px"
//         buttons.style.textAlign = "center"
//         if(typePlayer === 0){
//             buttons.innerText = `PVP`
//         }
//         else buttons.innerText = "PVE"

//        divButton.append(buttons)

//     }

// }
